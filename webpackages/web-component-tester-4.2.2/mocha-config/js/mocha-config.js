/* global mocha,chai */
/* exported assert,should,expect */
'use strict';
mocha.setup({ ui: 'bdd' });
// eslint-disable-next-line no-unused-vars
var assert = chai.assert;
// eslint-disable-next-line no-unused-vars
var expect = chai.expect;
// Note that should has to be executed
var should = chai.should();
